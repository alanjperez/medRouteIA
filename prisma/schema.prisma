// Prisma schema for the weekly medical-visit planner.

generator client {
  provider = "prisma-client"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model User {
  id           Int      @id @default(autoincrement())
  firstName    String
  lastName     String
  email        String   @unique
  passwordHash String
  phone        String
  pharmaLab    String
  // Sector asignado: departamento + municipio y, únicamente para el
  // municipio de Guatemala (ciudad capital), la zona.
  department   String
  municipality String
  zone         Int?
  createdAt    DateTime @default(now())

  doctors  Doctor[]
  sessions Session[]
}

model Session {
  id        String   @id
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  expiresAt DateTime
  createdAt DateTime @default(now())
}

model Doctor {
  id        Int  @id @default(autoincrement())
  userId    Int
  user      User @relation(fields: [userId], references: [id], onDelete: Cascade)
  name      String
  specialty String?
  address   String
  // Sector del médico: departamento + municipio y, únicamente para el
  // municipio de Guatemala (ciudad capital), la zona. Reemplaza latitud/
  // longitud como señal geográfica para el planificador.
  department   String
  municipality String
  zone         Int?
  phone        String?
  notes        String?
  // Maximum number of visitors (medical reps) the office attends per day.
  dailyCapacity Int      @default(1)
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  officeHours OfficeHour[]
  visits      Visit[]
}

// One row per day-of-week the doctor's office receives visitors.
model OfficeHour {
  id        Int    @id @default(autoincrement())
  doctorId  Int
  doctor    Doctor @relation(fields: [doctorId], references: [id], onDelete: Cascade)
  // 0 = Sunday ... 6 = Saturday
  dayOfWeek Int
  // Stored as "HH:mm" 24h strings.
  openTime  String
  closeTime String

  @@unique([doctorId, dayOfWeek])
}

model Visit {
  id            Int      @id @default(autoincrement())
  doctorId      Int
  doctor        Doctor   @relation(fields: [doctorId], references: [id], onDelete: Cascade)
  date          DateTime
  arrivalTime   String
  departureTime String
  durationMin   Int
  notes         String?
  createdAt     DateTime @default(now())
}
